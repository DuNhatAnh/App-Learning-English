version: '3.8'

services:
  frontend:
    build:
      context: ./frontend/english-pro---chinh-phục-tiếng-anh
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY:-PLACEHOLDER_API_KEY}
